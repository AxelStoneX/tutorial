#include <iostream>
#include <cstdio>
using namespace std;

int main(void)
{
   char str[80];
   char token[80];
   char *p, *q;
   
   cout << "Enter the sentence: ";
   gets(str);

   p = str;

   while(*p){
      
   }
}
