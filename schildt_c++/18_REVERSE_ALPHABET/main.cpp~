#include <iostream>
using namespace std;

int main (void)
{
   char letter;
   cout << "Here's reversed alphabet:\n";
   for(letter = 'Z'; letter >= 'A'; letter--)
      cout << letter << " ";
   cout << "\n";
   return 0;
}
